[pytest]
testpaths = playwright-framework/tests
python_files = test_*.py
markers =
    enq: ENQ module tests
    auth: login/session setup tests
    loggedin: tests that require a saved authenticated storage state
    module(name): module area under the suite
    smoke: critical smoke coverage
    regression: broader regression coverage
addopts = -ra
